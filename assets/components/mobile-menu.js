class MobileMenu extends HTMLElement {
  connectedCallback() {
    // This component is handled by main.js for simplicity
    // It provides the mobile menu toggle functionality
  }
}

customElements.define('mobile-menu', MobileMenu);